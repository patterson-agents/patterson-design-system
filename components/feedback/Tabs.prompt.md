**Tabs** — horizontal navigation with a sky underline on the active tab. Controlled or uncontrolled.

```jsx
<Tabs tabs={['Service','Logistics','Products']} onChange={setView} />
```

Accepts plain strings or `{value,label}` objects. Pair with your own panel rendering.

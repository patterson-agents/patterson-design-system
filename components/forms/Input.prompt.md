**Input** — labelled text field with a sky-blue focus ring and helper / error states.

```jsx
<Input label="Work email" type="email" placeholder="you@practice.com" required />
<Input label="Practice name" error="This field is required" />
```

Props: `helper`, `error` (turns border/ring red), `iconLeft`, `disabled`, `required`. 44px tall, 6px radius.

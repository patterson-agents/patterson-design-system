**Checkbox** — square check control; sky-blue fill with a white tick when on. Works controlled (`checked`) or uncontrolled (`defaultChecked`).

```jsx
<Checkbox label="Email me Patterson news" defaultChecked />
<Checkbox label="I agree to the terms" />
```

**Select** — restyled native dropdown with the brand chevron and sky focus ring.

```jsx
<Select label="Market" options={['Dental','Veterinary','Production animal']} />
<Select label="Region" options={[{value:'na',label:'North America'},{value:'uk',label:'United Kingdom'}]} />
```

Accepts plain strings or `{value,label}` objects.

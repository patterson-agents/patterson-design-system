# Font binaries — ground rule: no fonts committed. Licensing of self-hosted
# Proxima Nova is unconfirmed; fonts load via the Adobe Fonts kit or are
# restorable from the original handoff zip pending a license ruling.
*.woff2
*.woff
*.ttf

# claude.ai/design export artifact — not part of the deliverable.
.thumbnail

# Scraped production assets, pptx, and typekit CSS carried along for
# reference only — not part of the authored design system.
uploads/

# PPTX media dump scraped for reference during authoring — not part of the
# design system itself.
scraps/

# OS / editor noise
.DS_Store
*.swp

# Package-manager and build artifacts — this repo publishes `.`, so these
# must never be tracked.
node_modules/
dist/
.astro/
site/dist/
